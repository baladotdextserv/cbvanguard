export interface TariffItem {
  tariffItem: string;
  tariffDesc: string;
  tariffUnit: string;
  tariffImpPolicy: string;
  tariffImpCondition: string;
  tariffExpPolicy: string;
  tariffExpCondition: string;
}
